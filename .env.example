# ==========================================
# TEMPLATE DE VARIÁVEIS DE AMBIENTE
# ==========================================
# Este arquivo é um TEMPLATE (exemplo) sem dados sensíveis
# Copie para .env e preencha com valores reais: cp .env.example .env

# ----- DATABASE -----
# Desenvolvimento Local: Use SQLite (mais fácil)
DATABASE_URL="file:./prisma/dev.db"

# Produção (Railway): Use PostgreSQL
# DATABASE_URL="postgresql://user:password@host:port/database"

# ----- NEXTAUTH (Autenticação) -----
# Gere um secret forte com: openssl rand -base64 32
NEXTAUTH_SECRET="cole-o-secret-gerado-aqui"

# Local: http://localhost:3000
# Produção: https://seu-app.up.railway.app
NEXTAUTH_URL="http://localhost:3000"

# ----- NODE ENVIRONMENT -----
NODE_ENV="development"

# ----- EMAIL (SMTP) -----
# Configure um provedor de email

# Opção 1: Mailtrap (recomendado para testes)
# Cadastre em: https://mailtrap.io
# SMTP_HOST="sandbox.smtp.mailtrap.io"
# SMTP_PORT="2525"
# SMTP_USER="seu-username-mailtrap"
# SMTP_PASS="sua-senha-mailtrap"

# Opção 2: Gmail (precisa senha de app)
# Como gerar: https://support.google.com/accounts/answer/185833
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="seu-email@gmail.com"
SMTP_PASS="sua-senha-de-app-16-digitos"

# Opção 3: Ethereal (emails temporários de teste)
# Crie em: https://ethereal.email
# SMTP_HOST="smtp.ethereal.email"
# SMTP_PORT="587"
# SMTP_USER="usuario-ethereal"
# SMTP_PASS="senha-ethereal"

EMAIL_FROM="AgendaSalão <noreply@agendasalao.com>"

# ----- STRIPE (Pagamentos) -----
# Cadastre em: https://stripe.com
# Obtenha chaves em: https://dashboard.stripe.com/test/apikeys

# Use chaves de TESTE em desenvolvimento
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Use chaves de PRODUÇÃO apenas no Railway
# STRIPE_SECRET_KEY="sk_live_..."
# STRIPE_PUBLISHABLE_KEY="pk_live_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."

# ==========================================
# INSTRUÇÕES DE USO:
# ==========================================

# 1. DESENVOLVIMENTO LOCAL:
#    cp .env.example .env
#    Edite .env com seus valores reais
#    npm run dev

# 2. PRODUÇÃO (RAILWAY):
#    Não use arquivo .env no Railway!
#    Configure as variáveis no Railway Dashboard:
#    - Projeto → Serviço → Variables
#    - Adicione cada variável manualmente

# 3. SEGURANÇA:
#    - .env está no .gitignore (não vai para o Git)
#    - .env.example pode ir para o Git (sem dados sensíveis)
#    - NUNCA commite valores reais de senhas/chaves

# ==========================================
# VALORES PARA RAILWAY (PRODUÇÃO):
# ==========================================

# DATABASE_URL = (fornecido automaticamente pelo PostgreSQL do Railway)
# NEXTAUTH_SECRET = +SVcPHuRvto/Y1jb/irnG7lvcg5j9/RCNI8ud80JV1g=
# NEXTAUTH_URL = https://salao-production.up.railway.app
# NODE_ENV = production
# STRIPE_SECRET_KEY = sk_test_placeholder (ou chaves reais)
# STRIPE_PUBLISHABLE_KEY = pk_test_placeholder (ou chaves reais)
# STRIPE_WEBHOOK_SECRET = whsec_placeholder (ou chaves reais)
